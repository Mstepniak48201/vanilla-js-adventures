const formChange = () => {
  const form = document.getElementById("formStyle");
  form.style.color = "cyan";
};

const divChange = () => {
  const form = document.getElementById("divStyle");
  form.style.color = "yellow";
};

const pChange = () => {
  const form = document.getElementById("pStyle");
  form.style.color = "magenta"; 
};
